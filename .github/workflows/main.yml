name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  steps:
  java-test:
    uses: ./.github/workflows/java-test-reusable.yml

  docker-ci:
    needs: java-test
    uses: ./.github/workflows/docker-test-reusable.yml




